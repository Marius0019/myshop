{% extends 'base.html.twig' %} 

{% block title %}Accueil!{% endblock %}
    
{% block body %}
    <div class="card text-bg-dark">
        <img src="{{ asset('img/img_accueil.jpeg ') }}" class="card-img opacity-75 overflow-hidden" alt="...">
        <div class="card-img-overlay">
          <h1 class="card-title text-center">Bienvenue sur MYSHOP</h1>
          <div class="d-grid gap-2 col-3 mx-auto">
            <a href="{{ path('affiche_produit') }}" class="btn btn-secondary" type="button">Site e-commerce de prêt à porter</a>
          </div>
        </div>
      </div>
    
      {# {% for vehicule in vehicules %}
    
      <div class="card mb-3">
        <div class="row g-0">
          <div class="col-md-4">
            <img src="{{ asset('img/upload/' ~ vehicule.photo) }}" class="img-thumbnail rounded"   alt="...">
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title">{{ vehicule.titre }}</h5>
              <p class="card-text">{{ vehicule.description|slice(0, 100)}}</p>
              <a href="{{ path('dashboard_show', { id: vehicule.id }) }}" class="btn btn-outline-warning">Réserver et payer</a>
            </div>
          </div>
        </div>
      </div>
            {% endfor %}
           #}
      {% endblock %}
      
    